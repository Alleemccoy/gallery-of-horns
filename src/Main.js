import React from 'react';
import HornedBeasts from './HornedBeasts';
import './Main.css';
import hornedBeastsConstants from './constants/hornedBeastsConstants';

class Main extends React.Component {
  render() {
    // Next 4 lines are Tom's code from code review, just playing around and following along with code review
    // let beasts = [];
    // hornedBeastsConstants.forEach(item => {
    //   beasts.push(<HornedBeasts name={item.title} title)={item.title} imgUrl={item.image_url} description={item.description} />)
    // })
    return (
      <main>

      </main> 
    )
  }
}

export default Main;